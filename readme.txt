For compile the program write 
python lab.py -g

-To select a color to paint a cell with just right click the color that you want from the palette of the first column (the default is white)
-To paint a cell just left click the cell remember you can't paint any cell from the palette
